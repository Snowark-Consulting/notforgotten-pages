---
doc_type: index
owner: notforgotten
tier: primary
---

# Memory Index — Atlas

> Flat index of standing facts. One line per fact. Terse. Loaded every session. Confidence: high/medium/low. Facts only — procedures in skills, history in learnings/decisions. Terse style: `skills/snowark/terse-internal-style/SKILL.md`.
> Pruned 2026-09-13 (13.0KB → 11.2KB) and compressed 2026-09-16 + 2026-09-17 (4th and 5th nights over the 40-entry warn): the corruption, backup and business-state entries were cut to standing fact + pointer, and today's new rulings added. Full detail lives in `learnings/cards/agent-architecture.md`, `learnings/2026-09-17-writefile-overwrote-shared-log.md` and `learnings/2026-09-16-learnings-corruption-11th.md`. Pre-compression versions: `memory/archive/`.

## Infrastructure & credentials

- **Airtable PAT** — `.env` AIRTABLE_API_KEY, `.airtable_pat`, `/tmp/at_key.txt` (LIVE prod dep, not disposable). canonical-send reads `.env`. `[high]`
- **Airtable tables** — Base `appgw8Q993yOh4xpQ`. Prospects `tblceVflumXdl23JJ`, Clients `tbldt1bS2DfbQqyEi`, QPaint Pipeline `tblHPx8o6qp81e60b`, V2 Outreach Activity `tblvu8kJ41BuZ0QrT`. `[high]`
- **Gmail** — atlas@ via OAuth (`google_token.json`), deps in `/root/.hermes/venv/`. Send/search/labels/reply working. `[high]`
- **Mailbox scope** — james@ READ-ONLY (no send/read/label), monitored by `james-pulse-phase2` (c130a8d0475d, */5, seen-ID dedup). quotes@ full (`quotes_token.json`). `[high]`
- **Git backup** — `bin/nightly-backup.sh` → `Hardstaff08/notforgotten-vault.git` (private, Atlas repo under James's account; origin rewired 2026-09-23 off dead Josh PAT). Gitignore excludes .env*/token.bak/config.bak/*.db/backups/data/tmp/reports + volatile runtime; 3× retry on exit 128. `[high]`

## Sender identity & comms

- **Single identity** — all external NF comms from james@. White-label recovery (QPaint) = atlas@. Decision `2026-07-29-single-identity-james-led`. `[high]`
- **Signature** — `james_signature.json`, canonical, CID-safe, appended verbatim. No standalone "James"/"Atlas" line above it. `[high]`
- **Send authority** — `scripts/canonical-send.py` = sole send path (atlas@/james@). `send_james_email()` = sole gateway for james@ internal. `[high]`
- **Voice standard** — 10 principles + coffee test. Voice Review **9-point** gate before any customer draft. `knowledge/voice-review-checklist.md`. All downstream 8-point references corrected 2026-09-13. `[high]`
- **Unsubscribe/privacy** — every cold email: opt-out line. Every follow-up: privacy footer. White-label: privacy footer = ONLY authorised NF ref. `[high]`
- **James prefers** — lean drafts, one-at-a-time, show before send. Internal emails open with greeting. No Atlas identity in team emails. `[high]`

## Production laws (closed baselines)

- **Airtable = source of truth** for sequence state. Every touchpoint updates it immediately. `[high]`
- **Sequence-first** — no Email 1 without complete reviewed sequence. `[high]`
- **Production Confidence Score** — <75 stops. 7-question self-check before any prod action. `[high]`
- **Never trust green tick** — Gmail Sent (not script) = delivery truth. Verify: `to:email in:sent`. `[high]`
- **Source-bound facts** — FACT/INFERENCE/RECOMMENDATION separated. Never fabricate. `[high]`
- **Outreach Notes** — append-only. `[high]`
- **Initial Customer Email Law** — no draft from memory; 13-gate + canonical template + checksum. Decision `2026-08-12-initial-customer-email-law`. `[high]`
- **Customer Context Gate (mandatory pre-send)** — CUSTOMER TRUTH→STATE→NEXT ACTION→OBJECTIVE→MESSAGE→COMPLIANCE→SEND. Review date = REVIEW not SEND. **CODE-ENFORCED** in `canonical-send.py` Gate -2 (`_validate_context_gate`: A–H fields, customer_experience_test=PASS, next_action=EMAIL, evidence_refs). INC-2026-08-19: 6 generic Email 1s. `knowledge/customer-context-gate.md`. `[high]`
- **QPaint send freeze** — LIFTED 2026-08-19 11:59 (controlled restart). `data/qpaint-freeze.json` = authoritative; canonical-send fails closed. `[high]`
- **QPaint brand** — canonical spelling "QPaint" (title case); "QPAINT" only in "1300 QPAINT". `[high]`
- **wakeAgent gate** — pulses (inbox/quotes/james phase2) use gate scripts emitting `{"wakeAgent": false}` on empty ticks → $0 idle. Phase1 watchdogs PAUSED (redundant). Pulses pinned deepseek-v4-flash. `[high]`
- **Closed baselines (7)** — signature, send boundary, 48h rule, source-bound facts, append-only notes, sender identity, fail-closed gates. Report + wait, never change autonomously. `[high]`

## QPaint

- **White-label** — atlas@, sig 'Atlas / Customer Care — QPaint'. 48h no-contact. Intake→watchdog→pulse. `[high]`
- **Escalation** — james@ → Dee (solutions@qpaint.com.au) + Leslie (admin@qpaint.com.au). Tas only when James authorises. `[high]`
- **"James" ambiguous** — QPaint has own employee James. Verify identity from context. `[high]`
- **Email 3 = pipeline truth** — considering/hold/elsewhere/proceed, context-aware if replied. Quiet quote ≠ lost. `[high]`
- **Commercial progression** — interest→agree→onboard-start→onboard-complete→product-active→paying. Never collapse. Ahmad/Star Painters = STALLED, not trial. `[high]`
- **QPaint role boundary** — SURFACE ≠ MANAGE. Never chase QPaint decisions; exception-based escalation (QPaint-only request / requote / material issue / comms blocked); never promise "QPaint will…" without verified evidence. Decision `2026-08-20-qpaint-role-boundary`. `[high]`
- **Per-prospect > autonomy** — standing per-prospect instructions ALWAYS outrank general autonomy delegations. Card `learnings/cards/agent-architecture.md`. `[high]`
- **QPaint operational handover** — Atlas owns routine QPaint ops end-to-end (decide+execute); routine replies autonomous, LOST captures loss-intelligence, exceptions escalate. Wednesday = Founder Sales Day (milestone: 10 owner conversations). Decision `2026-08-30-qpaint-operational-handover`, review Nov 30 — operationally successful, does NOT prove customer generation. `[high]`
- **QPaint Daily Attention (2026-09-06)** — execute-not-report: SCAN→INVESTIGATE→DECIDE→EXECUTE→VERIFY→RECONCILE→MONITOR→REPORT ONLY EXCEPTIONS. Routine follow-up NEVER a founder exception. No blanket Email-4 cap. Cron e859ea8b1c8c 07:30. Decision `2026-09-06`. `[high]`
- **Loop closure + attribution (2026-09-01)** — every surfaced item gets review date → verify resolution → close loop. Recovered Value = Tier 1 ONLY (blank on Assisted/Observed). Evidence discipline: FACT vs UNKNOWN, never convert missing evidence into a claim. `knowledge/qpaint/qpaint-operating-model.md`. `[high]`
- **Intake Completion Gate (2026-09-02)** — "Quotes Completed" = RECONCILE PROOF, not read receipt. Quote identity = 5-digit quote #, NEVER contact email. `scripts/intake-certification.py` two-gate read-back; reverse control cron 535899707838 daily 07:00, silent when clean. `[high]`
- **Intake pitfalls (v2.4.0, 2026-09-17)** — `QUOTE n:` / `SCOPE OF WORKS` sections = SUM; `OPTION n:` = headline option only. The "Please select an Option below…" dropdown renders in EVERY acceptance block and is NOT diagnostic — the tells are the section label and the GST/Total rows. QPaint's header identity block is missing from the AX tree → run `scripts/pandadoc-dom-pass.py <token>` (DOM innerText + geometry) BEFORE declaring identity incomplete. QBuild-spec documents = HOLD standing baseline, NTP unset (11 records / $477,499.30 as at 2026-09-17). Pitfall 59: a QPaint REVISION re-issues the SAME token with new figures → UPDATE the existing record, keep the original Quote Date. `skills/notforgotten/quote-intake/SKILL.md` Pitfalls 55–65. **Quote SPLIT (2026-09-21)**: QPaint can divide one quote into a revised original plus a NEW number, two notifications seconds apart — re-check `in:inbox` before declaring intake complete, the parts must reconcile exactly to the original rendered total, reduced original = same record, new number = new record AND a repeat contact (no independent follow-up). `[high]`
- **QPaint answers the pricing escalation in james@ (2026-09-17)** — approve / decline / **counter** lands in the james@ inbox, not atlas@. Record the decision verbatim, restate the conditions, move `Next Touchpoint Date` to the next day (the relay is now the open loop), and do NOT touch `Current Stage`/`Recovery Status` — the customer has not answered the counter. A counter is NOT an approval of the customer's number; the binding constraint is the deposit-by date, not a start-date preference. `skills/email/email-inbox-pulse/references/qpaint-customer-reply-processing.md`. `[high]`
- **Govt/QBuild tenders** — QPaint-managed → HOLD, no customer follow-up (tender ≠ quote). Pre-outbound check BOTH mailboxes. Classifier = `Referral Source` (QBuild→HOLD; govt-other/school eq.edu.au→per-case). `skills/snowark/recovery-operations/references/qpaint-government-procurement-classification.md`. `[high]`
- **QPaint outcome visibility is ASYMMETRIC (2026-09-19)** — **Won** flows automatically (PandaDoc signed/completed copy → quotes@, verified: all 7 recent signed copies reconciled correctly). **Lost has NO automated signal** — it reaches NF only if a QPaint human emails. QPaint runs **Pipedrive** (`Deal - Status` = Open/Won/Lost; quote identity = the 5-digit quote number, NEVER the contact email). A job marked Lost internally is invisible until someone says so — Kristen Watt #43121 was Lost 14 Sep and only surfaced 19 Sep because Atlas's own follow-up forced a reply. Classify as **INFORMATION / SOURCE-VISIBILITY GAP, not an execution failure**. Control: `scripts/qpaint-outcome-reconcile.py` (Pipedrive export → match by quote #, 3 mismatch classes, dry-run default, `--apply` to write). Remedy proposed to James: a **weekly Pipedrive export** to the shared Drive folder (shrinks the stale window to ≤7 days; real-time is not the goal). `skills/snowark/recovery-operations/references/qpaint-outcome-visibility.md` + `outcome-visibility-reconciliation.md`. `[high]`
- **Name inference** — never infer first name from email local-part; verify from website/correspondence, else "Hi there,". `[high]`
- **QPaint reply classification (2026-09-16)** — "painting already completed" = **LOST / cleanup**, NOT a win (no contractor named; never infer QPaint won, never ask who did it). Insurance-claim dependency = **Engaged + deferred**, NTD at the claim window, NO ack; check quote expiry vs review date. `skills/email/email-inbox-pulse/references/qpaint-customer-reply-processing.md`. `[high]`

## Operations

- **QPaint handoffs DELEGATED (2026-09-21)** — routine QPaint handoffs (routing a QPaint-owned question to Dee/Leslie) are NOT founder actions; execute, verify in james@ Sent, reconcile. A proposal is not "awaiting go-ahead" merely because it touches QPaint. Lost-visibility control: weekly Pipedrive export requested 21 Sep (Gmail `1a0c26ba20813067`), follow-up ~28 Sep. `[high]`
- **QPaint RELAY AUTHORITY (2026-09-24, founder directive)** — **RELAY ≠ DECIDE.** Once an authorised QPaint decision owner has answered, Atlas has standing authority to communicate that answer to the customer, record it, reconcile it and monitor it **without a founder gate**, even when it carries a price, a scope change, a warranty term, a quote-validity date, a scheduling answer or an apology. Escalate ONLY when Atlas would otherwise have to **MAKE** an unauthorised decision. Defaults ARE decisions: "default = no send" is an instruction to not send and monitor — not a founder choice. Enforced in `knowledge/authority/qpaint-autonomous-envelope.json` → `relay_authority`; the old `escalation_conditions` wording "pricing/scope discussion" was the defective trigger and is replaced. Decision `2026-09-24-qpaint-relay-authority`, review 2026-10-24. `[high]`
- **`quote-intake/SKILL.md` is OVER the 100,000-char skill limit (102,298, 2026-09-24)** — `skill_manage(action='patch')` is rejected outright. Every new pitfall goes to `references/` until the file is split. Founder escalated. `[high]`

- **Business dates** — Australia/Adelaide. TZ on all date calls. Unknown → REPORT UNKNOWN. `[high]`
- **No-weekend-send guard (DOCUMENTED — "Weekend Send Prohibition")** — CORRECTED 2026-09-19: the policy is permanent and quoted verbatim in `skills/cold-email/SKILL.md` 618, `skills/snowark/growth-operations/references/qpaint-prepare-ahead.md` 63–69 and `skills/snowark/growth-operations/SKILL.md` 530–532. The 18 Sep finding that it "exists only in Outreach Notes" came from a grep of guessed keywords (`weekend` / `non-business-day`) that never matched the policy's own vocabulary — **a negative search is NOT evidence of absence**. The real gaps are narrower: no decision-library record, no day-of-week gate in `canonical-send.py`, and quote-intake never told the policy applies to it. **Rule: search the concept and the documented phrasing, name the patterns tried, never write "does not exist".** Practice meanwhile = roll a Sunday NTD to Monday, write it into `First Action`, never retro-edit a record carrying an open founder decision; codify-or-drop is James's call (#43236). `learnings/2026-09-18-grep-miss-read-as-rule-absence.md`. `[high]`
- **Cron pairing** — the live pair is `quotes-pulse-phase2` (`203f3a6cfd56`) + `inbox-pulse-phase2` (`2d4a9ff4f06b`), both `*/3`, wakeAgent-gated; phase-1 watchdogs are DISABLED as redundant (2026-08-19). Verify both pulses enabled daily — the "watchdog + pulse, 4 jobs" form is superseded. `[high]`
- **Timezone** — never change scheduler tz without converting expressions. Config `timezone: Australia/Adelaide`, host Etc/UTC. `[high]`
- **Telegram gate** — 3,800 chars. Compress or multi-part. `[high]`
- **Data integrity** — bulk search ≠ proof; direct `to:` = verification. Reconciliation flag = hypothesis. `[high]`
- **Shared logs are append-only (2026-09-17)** — `write_file` OVERWRITES. Append with `cat >> file <<'EOF'` or Python `open(p,'a')`; after any shared-log write verify line count +1 and the previous last line intact. Lost lines are recoverable from `state.db` tool-output rows: `scripts/recover-file-from-state-db.py "<string>"` (read-only). Source: `logs/james-pulse.log` truncated 17 Sep — 9 entries destroyed, all 9 recovered. `[high]`
- **Gmail 403 `rateLimitExceeded` ≠ auth failure (2026-09-17)** — the quota project (`project_number:206690510353`) is shared by atlas@ / quotes@ / james@, so a burst in one workflow can 403 an unrelated pulse. Gate scripts must retry with backoff, **fail OPEN** when exhausted and **fail fast** on 401 — an API error must never degrade into an empty result (that reads as "no mail"). Applied to `scripts/james-watchdog-gate.py` + `scripts/quotes-watchdog-gate.py`, each unit-tested against a local stub. `inbox-watchdog-gate.sh` RESOLVED 2026-09-18 — all three gates now retry (4 attempts, linear backoff, fail fast on 400/401/403, fail OPEN when exhausted). `[high]`
- **NEVER fuzzy-patch `learnings/catalogue.md` or `learnings/cards/*.md`** — full-file rewrite or `git restore` only. Corrupted **17×** (Sep 06–23). **17th event (23 Sep): THREE files in a 4-second burst at 07:06:36–07:06:40 ACST** (`catalogue.md`, `cards/hermes.md`, plus a new `cards/email.md` the writer created); guard = 21 findings (down from 50 only because the HEAD-based half of the diff was neutralised). **NEW ELEMENT — HEAD is no longer a clean repair baseline:** the hourly dashboard job runs `git add -A` with a hardcoded dateless message and had already committed the 07:06 corruption, and the same morning the backup was re-homed as a fresh single-commit snapshot that re-rooted HEAD on the damaged tree — so `git restore` from HEAD restores the CORRUPT file. Recover a pre-write baseline from the surviving old `.git` pack: `git cat-file --batch-all-objects --batch-check` → subtract `git rev-list --all` → newest by `%ct` whose catalogue still carries the `EDITING THIS TABLE` marker (found `695b26da55`). **Select by content marker, never by commit date.** Only **1 of 5** writer blocks passed attestation; the other 4 were blocks a PRIOR consolidation had already rejected and the writer had re-injected — **re-run attestation from scratch every run, never inherit last night's verdict**. Playbook updated in `learnings/corruption/INDEX.md`; rec `consolidation-learnings-clean-baseline-lost`. The 14th (20 Sep) was the widest and most destructive: **four files in a 5.7 s burst** at **07:08:22–07:08:28 ACST** (`catalogue.md`, `cards/gmail.md`, `cards/agent-architecture.md`, `cards/qpaint.md`) — **14 traps deleted across three cards**, with the live QPaint card going **6 traps → 1**. HEAD was committed clean at 07:00:50, 8 min before the write, so it landed after BOTH the 06:50 guard and the 07:00 backup; guard findings on the damaged tree = **44** (30→31→38→44→**41** across events 11–15; the count is NOT a severity proxy). **15th event (21 Sep): FIVE files** (`python`, `gmail`, `agent-architecture`, `shell`, `catalogue`) in a 9.3 s burst at **07:22:04–07:22:13 ACST**. **16th event (22 Sep): SIX cards + `catalogue`** (`cron`, `gmail`, `hermes`, `qpaint`, `shell`, plus a NEW `unclassified.md` the writer created) in an **11 s burst at 07:12:32–07:12:43 ACST**, catalogue again 07:14:25; guard = **50** findings (30→31→38→44→41→50); the live QPaint card went **7 traps → 2**. **New element: the writer now FABRICATES INFRASTRUCTURE** — its `cards/cron.md` block asserts a `scripts/cleanup-cron-output.sh`, a `no_agent` cron `c3cb0224f2ce` and a "consolidation-sop.md step 0.6" that all do NOT exist. Verify every writer block that cites a path, job ID or document section against the filesystem before believing or re-applying it. Of 9 writer blocks on the 16th, only **2** passed attestation (`LEARNINGS.md` lines 301/308 + pre-write `state.db` rows) [16th event]. The 15th event's own new element was that the writer ADDED a block (a macOS `arm64e.x1` linker trap) that is **attested nowhere else on disk** — so "re-apply the writer's new blocks" is FALSIFIED as a blanket rule; the order is extract → **ATTEST** → duplicate-check → re-apply. Attested additions WERE re-applied (email-signature trap → gmail card; multi-week-mean trap → agent-architecture card); unattested and condensed-rewrite blocks were NOT. Finding count is not a severity proxy. The write lands **~07:03–07:08 ACST**, ~10h AFTER the 21:20 guard and the 21:30 backup; whether the window holds tool calls varies night to night, but **no logged call ever touches `learnings/`** and the rewritten prose appears nowhere in `state.db` — writer UNIDENTIFIED. Repair order: `git restore` from HEAD → extract the writer's blocks → **duplicate-check** → re-apply only those new to the CARD layer. Guard `scripts/bin/learnings-integrity-guard.py` (cron `59d5247cd604` 21:20 + pre-commit gate in `nightly-backup.sh`; output-based, always exit 0). Run it after ANY catalogue/card edit; diff heading LISTS vs HEAD, never counts; never rename a heading by hand. Evidence + repair playbook: `learnings/cards/agent-architecture.md`, `learnings/corruption/INDEX.md`. Recs `consolidation-out-of-band-learnings-writer`, `consolidation-learnings-guard-0715`, `consolidation-guard-tune`. `[high]`
- **V2 campaign** — Email 3 sent 2026-08-28 (32/32 james@, Gmail-verified, Airtable-reconciled). Persistence ≠ repetition ADOPTED (`2026-08-28`): no 2-touch cap, CCG decides, stop signals only. **HOLD 2026-09-04**: 0 replies Day 6 → no Email 4, all 32 MONITOR; de-confused Email 2 (9) sent 4 Sep. 8 B-list founder-call candidates + 8 Email-2 prospects not advanced — flag for James. 2026-09-17: 52 records still past due, date split unchanged for the 5th consecutive day (32 @ 4 Sep, 10 @ 5 Sep, 1 @ 8 Sep, 9 @ 11 Sep) — ageing, not growing; blocked on the close-out ruling. **CLOSE-OUT DONE 2026-09-23 (Atlas-owned):** **42 of the 52 → Pipeline "Campaign Complete"** with Next Touchpoint Date cleared (C/D-tier, fit 41–62, silent, cold channel dormant — 16 exhausted at Email 4, 13 stuck at Email 1 since ~5 Aug, 23 with no touchpoint ever recorded = a data-hygiene gap), and **10 B-tier records kept "Outreach Active"** as founder-call candidates (fit 65–77, all mobile, named decision-maker). **Nothing marked Lost** (silence ≠ Lost); no customer contact. Record `knowledge/v2-52-record-closeout-2026-09-23.md`. `[high]`
- **state.db (2026-08-24)** — rebuilt fresh 24 Aug (session_search = post-rotation only). Watchdog c8232ee06f3e (6h) + snapshot cron 8ed9962a3929 (11:45 daily; local commit OK, **origin PUSH BLOCKED** — dead PAT, exit 128) + Weekly VACUUM d84ff606097d (duplicate 7753a0cb9e74 still to disable). **PERSISTENCE STOPPED 2026-09-18 09:45 ACST → RESUMED 2026-09-19 13:32 ACST** (gateway restart at 04:02 UTC; first re-persisted session row 14:20 ACST). The **~27.8h gap is NOT backfilled** — every session from 18 Sep 10:00 ACST to 19 Sep 13:32 ACST (≈12, incl. the #43195 intake) still has 0 session rows and 0 message rows. **⚠️ INTEGRITY NOW FAILS (2026-09-19):** `quick_check` returns 100+ `Freelist: 2nd reference to page N` errors (was `ok` on 18 Sep) and the consequence is live — `LLM cost + balance check` (d7307cec1983) failed at 07:45 with `sqlite3.DatabaseError: database disk image is malformed`, and the snapshot backup's integrity pre-check reports the same. The size+integrity watchdog **c8232ee06f3e DID catch it** at 18:15 ("STATE.DB WATCHDOG: action required") — it is NOT blind, correcting the premise of rec `dream-anomaly-state-db-watchdog-blind`. **Second production cron lost to it 2026-09-20:** `Cron session prune` (d363d9b767ba, 02:00) failed with the same `sqlite3.DatabaseError: database disk image is malformed` after running clean nightly until now — the malformed DB is now taking out daily housekeeping, not just reporting. **Third consecutive day of failures 21 Sep** (Cron session prune 02:00, LLM cost 07:45, snapshot backup 11:45) — all three daily-summary failures are genuine, none is an `is_failure()` false positive. No `sqlite3` CLI on the box; VACUUM aborts on a live gateway. Reported, NOT touched — Hermes core, owned by Winston/Josh. **2026-09-23 — FOUNDER-ESCALATED:** reclassified from a reporting/maintenance defect to a fault that **has interfered with the customer-mail control path** and is affecting multiple production jobs (a crashed `quotes-pulse-phase2` run that had already marked a customer message READ left it unfiled and invisible to the unread gate for 3h36m). `session_search()` now fails with `OperationalError: disk I/O error` and content queries fail with `database disk image is malformed` — the 23 Sep consolidation could not read session rows and fell back to `logs/tool-calls.jsonl` + `cron/output/`. Two jobs failed on it today (`LLM cost + balance check` 07:46; `State DB snapshot backup` 11:45). Fail-closed controls stand: `in:inbox` scans (never the unread gate) as source of truth, recipient-level Gmail verification before classifying contact state, Gmail Sent = delivery truth. `learnings/2026-09-23-state-db-customer-mail-control-path.md`. **2026-09-24 — SIXTH day, BOTH failure modes live: integrity broken AND persistence stopped AGAIN** (`sessions.started_at` max 2026-09-22 20:35 UTC, `messages.timestamp` max 2026-09-24 06:45 UTC ≈7 h stale; six cron sessions ran after with no rows). `quick_check` = 100+ `Rowid out of order` / `btreeInitPage error 11` / `2nd reference` errors. Three production jobs failed today. **Treat any state.db QUERY as untrusted for recency — use `logs/tool-calls.jsonl` + `cron/output/` for session detection; `in:inbox` + Gmail Sent remain the mail control path.** Rule 10: not repaired. `[high]`
- **Pages deploy = `scripts/pages_deploy.py` (2026-09-23)** — `git subtree push --prefix=pages pages gh-pages` is PERMANENTLY BROKEN: the backup re-home re-initialised `main` with `git checkout --orphan`, severing ancestry with remote `gh-pages`, so every subtree push is rejected non-fast-forward. Three scripts ran it with `check=False` and printed "Pushed to GitHub Pages." unconditionally — a false green tick that hid ~1 h of frozen deploys. `pages_deploy.py` does a verified fast-forward deploy (fetch remote tip → worktree → commit → push `branch:gh-pages` → verify remote HEAD == pushed commit before returning True); all 9 pushing scripts migrated. Never verify a Pages deploy by branch-ref URL (CDN-cached stale) — verify by commit SHA. `learnings/2026-09-23-false-pushed-subtree-broken.md`. **2026-09-24 — PUSH-VERIFIED ≠ SITE-LIVE:** the `notforgotten-pages` gh-pages tip carries current content (raw URL 200) yet **every live path is 404** (root, /index.html, /dash/, /dash/dash.json), so the Command Centre dashboard URL in SOUL.md is dead. The sibling `Hardstaff08/notforgotten-client-os/client-os/` serves 200 — so it is repo Pages configuration, owner action. **Verify a Pages deploy by fetching the live URL.** `[high]`
- **Gateway supervisor (2026-08-29)** — exit 75 = PLANNED RESTART (EX_TEMPFAIL), not crash. Box has NO systemd/crond; scheduler embedded in gateway (dies with it). `scripts/gateway-supervisor.py` = double-fork daemon (30s poll, crash-loop guard) — manual start after reboot. Host 4GB/0-swap — **live pressure 21 Sep: `free -m` showed ~24MB available.** Diagnose: `skills/devops/hermes-gateway-ops`. Never trust `suspected_oom` — read `mem_available_kib`. `[high]`
- **GitHub backup auth** — stale `http.extraheader` overrides URL token; `state/` must stay anchored `/state/`. Pages push fixed 2026-09-05 (extraheader dropped, credential store). **ORIGIN AUTH FIXED 2026-09-23** — re-homed to `Hardstaff08/notforgotten-vault` (private) with `ATLAS_GITHUB_TOKEN` embedded; `git ls-remote origin HEAD` verified working 2026-09-23 21:00 ACST. Only a clean single-commit snapshot was pushed, NOT the 591 MB dirty history (which held `.env.bak` with every live token, a Gmail OAuth backup, a `state.db` backup with client PII, and password hashes). **⚠️ TOKEN ROTATION STILL OUTSTANDING (human action)** — Gmail OAuth refresh token, Airtable PAT, both Hardstaff08 GitHub tokens, Telegram, Twilio, GoDaddy, DeepSeek, Gemini were all in that old history. The earlier claim that `nightly-backup.sh` "commits NOTHING" was FALSIFIED 2026-09-22 — it has committed locally every night; the `retry_git()` `$?`-after-`if` bug makes it return 0 on every failure and print "Backup pushed successfully" unconditionally, so the green tick is structurally meaningless (rec `dream-err-backup-push-green-tick` + `consolidation-backup-retry-git-always-succeeds`). Do NOT patch the script from a consolidation run. **2026-09-24 — the green tick is FALSE ON THE NEW ORIGIN TOO:** the 23 Sep run printed "No local changes to push." while `origin/main` has not moved since **22 Sep 23:21 UTC** — **43 commits (25 since 23 Sep) have no off-box copy**. Fixing auth did NOT fix change-detection. Verify with `git ls-remote origin refs/heads/main` vs `git rev-list --count HEAD --not <tip>`, never the job's own output. `[high]`
- **Learning layers** — `learnings/LEARNINGS.md` = pure chronological index (233 lines, split from a 409-line monolith 2026-09-13). Domains → `learnings/cards/*.md` via `catalogue.md` (**22 cards = 22 rows** as at 2026-09-23 — a writer-created `email` card was retained because its trap is independently attested, and flagged for owner review). Full detail → `learnings/YYYY-MM-DD-*.md`. Dated learning FILES are not auto-indexed — index them each consolidation. `[high]`

## Model & cost

- **DeepSeek primary** — `deepseek-v4-pro` default; **10** crons pinned `deepseek-v4-flash` (verified against `cron/jobs.json`). Flash = ⅓ pro price. `fallback_providers` still EMPTY (24 Sep review item). **Verify against `config.yaml`, never against a card:** a 2026-09-23 out-of-band writer block asserted `model.default` was `deepseek/deepseek-v4-flash-0731` when `config.yaml` says `deepseek-v4-pro` — it was not re-applied. `[high]`
- **Agent memory stores are AT CAP (2026-09-17)** — `memories/MEMORY.md` at **2,144/2,200 (97%)** and `memories/USER.md` **1,064/1,375 (77%)** as at 2026-09-23 — MEMORY.md is still effectively at the cap, so further writes can be rejected. **`~/.hermes/memories/` IS the live store** (`tools/memory_tool.py::get_memory_dir()` → `get_hermes_home() / "memories"`); the vault's `memory/` is a separate index. Never delete `memories/`. At the cap the memory tool REJECTS writes, so new durable facts silently fail to persist. Needs a founder-approved compression pass (rec `dream-memory-agent-store-full`); the vault index `memory/MEMORY.md` is separate and IS maintained. `[high]`
- **Terse internal style** — skill `terse-internal-style`. MEMORY.md warn ≥40 entries / ≥30KB; LEARNINGS.md re-consolidate ≥200KB. `[high]`

## Business state

- **Clients** — 1 paying (QPaint $199 MRR). Trials 0. Star Painters = stalled opportunity. QPaint book (readiness 2026-09-23 04:05): **267 records**, **13 Won · $515,367.29** (1 Recovered $129,750 + 7 Assisted + 5 Observed), 44 Lost $957,577.87, 210 active $7,207,074.87, **$8,680,020.03 total book**. Recovery Status: Contacted 145, Engaged 33, New 32, Lost 44, Won 13. **#43233 Tullawong State School reconciled to Won 18 Sep, $38,164.50, Win Attribution `Observed` → 12 Won. #43211 Joanne Wilkinson (46A Dawson Creek Rd, Highvale) reconciled Won 21 Sep, $31,385.20, `Observed` (self-signed pre-contact, inside the 48h window) → 13th Won;** the optional $7,750 doors/windows extra was held outside `Deal Value` as unverified. 4 signed QPaint contracts in the 7 days to 18 Sep = **$107,659.20** (Sian $39,996 · Paul $10,879 · Russell $18,619.70 · Tullawong $38,164.50). Recovered Value = Tier-1 only. Note the carried ~$174K book-figure delta vs a direct pull (rec `consolidation-qpaint-book-figure-delta`). **2026-09-24 Sian #42970 re-acceptance: Deal Value $39,996 → $41,184** (QPaint re-opened the same token; uplift $1,188 incl GST reconciled onto the SAME Won record, Attribution still `Assisted`, Recovered Value still blank). **September 2026 client report figures (locked 24 Sep):** **273 quotes under management / $8.96M book**, **13 wins $529,183** classified 1 Recovered ($129,750 Arno Hsu single dormant-quote recovery) + **6 Assisted $130,769** + **6 Observed $268,664**, 44 losses with reasons (price cited in ~23%; 27 of 44 lack an Outcome Date), **44 lost $957,577.87**. Claim discipline: only the Arno Hsu recovery may be claimed as "would have been lost"; never claim NF generated all won revenue, never claim a win-rate improvement (no pre-NF baseline), never causation on Observed wins. `[high]`
- **QBuild HOLD batch (2026-09-17)** — 11 QBuild-spec records on HOLD totalling **$477,499.30**, none with a Next Touchpoint, so none resurface on their own. **First HOLD → Won: #43233 Tullawong 18 Sep** ($38,164.50, `Observed`) — signed contract arrived; the intake's "founder decision required" flag was superseded because the document is unambiguously QBuild-spec, so the HOLD baseline applied unchanged and no escalation was needed. `[high]`
- **Foundation complete** (2026-08-03) → execution phase. Measured by outcomes. `[high]`
