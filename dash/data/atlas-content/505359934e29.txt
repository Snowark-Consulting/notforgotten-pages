---
doc_type: decision-review-prep
owner: notforgotten
tier: primary
date: 2026-09-17
status: PREPARATION — review due 2026-09-24 (7 days out). Verdict is James's.
prepared_by: Atlas (Prepare-Ahead Clock 22:00 ACST)
---

# Review Prep — Provider Failover Deferred (due 24 September 2026)

> Outcome data gathered ahead of the registered review date for `2026-08-24-provider-failover-deferred`.
> This is a REVIEW of an infrastructure posture. No customer contact, no send, no config change is made by this prep.
> This prep did not exist before tonight. The 24 Sep review had no preparation on file.

## The decision being reviewed

**Slug:** `2026-08-24-provider-failover-deferred`
**Decided 24 Aug 2026:** Provider failover deferred. DeepSeek judged healthy; `fallback_providers` left empty. The drift from `2026-07-24-provider-failover-gemini-fallback` was acknowledged as an accepted operational risk, not an unnoticed gap.
**Review date:** 2026-09-24.

**What this review is asking:** is the single-provider posture still a conscious accepted risk, or has the evidence moved far enough to change it?

## Renewal status — re-verified 17 Sep 2026 22:00 ACST

| Check | Result | Evidence |
|---|---|---|
| `fallback_providers` in config | **still `'[]'`** — unchanged for 24 days | `config.yaml` line 9 |
| Parses as a fallback chain | **No.** The value is a quoted **string**, not a YAML list. This is the documented `hermes config set` list-storage trap | `Agent/registry` → `growth-operations` Pitfalls |
| Primary provider | `deepseek` / `deepseek-v4-pro` | `config.yaml` `model:` block |
| Crons pinned to the cheap tier | **10** jobs on `deepseek-v4-flash` | `cron/jobs.json` (verified) |
| Cron jobs reporting non-ok | **1 of 42** — `State DB snapshot backup` `8ed9962a3929`, exit 128 | Dead origin PAT. Not provider-related |

## Did a failover event occur in the 24 days since?

**No event required failover.** DeepSeek produced 27 logged transient API failures across three days, every one absorbed by the built-in retry:

| Date | Logged API-call failures | Outcome |
|---|---|---|
| 2026-08-24 | 6 | All retried (`attempt 1/3`), recovered |
| 2026-08-25 | 16 | All retried, recovered |
| 2026-08-27 | 5 | All retried, recovered |
| 2026-08-28 → 2026-09-17 | **0** | 21 consecutive days clean |

Error types logged: `ReadError` (`[Errno 32] Broken pipe`), `APIConnectionError` (`Connection error.`), `APIStatusError`. Retry policy `default`, interval ~2s.

**No line in the gateway log shows an exhausted retry, an "all attempts failed" outcome, or a fallback being invoked.** No cron job failed for a provider reason. No user-visible degradation was reported.

## What is NOT a provider incident (careful attribution)

- **2026-08-29, 8.5-hour overnight outage.** This was a gateway **process** failure — memory exhaustion and an exit-75 planned-restart signature — not a DeepSeek outage. Full diagnosis: `skills/devops/hermes-gateway-ops`. Recorded here so a future reader does not count it as provider evidence.
- **Telegram "Bad Gateway" reconnects (26 Aug).** Telegram transport, unrelated to the model provider.
- **`State DB snapshot backup` exit 128.** Dead embedded PAT on `origin`. Unrelated to DeepSeek.

## The resilience argument that changed since 24 Aug

The `wakeAgent` gate was deployed 2026-08-18. Empty ticks emit `{"wakeAgent": false}` and the agent is skipped — **$0 idle**. That was a cost win, and it also changed the failure signature:

| Before the gate | After the gate |
|---|---|
| A provider outage caused repeated cron error spam into the alert channel | An outage surfaces as **missed pulses** — quiet, with no alert at all |

The original containment goal from the Jul-24 decision (stop the spam) is now met by the gate, not by a fallback chain. But the same gate **removes the alarm**. A multi-hour DeepSeek outage would now delay `inbox-pulse-phase2` and `quotes-pulse-phase2`. Those are the paths that detect a QPaint customer reply. A silent detection delay is a **service-continuity risk**, which SOUL Principle 13 places above provider loyalty.

This is the substantive new evidence for 24 Sep, and it cuts both ways: the gate solved the noise, and it created a new blind spot that a fallback chain would partially cover.

## Verdict options for review (James decides)

1. **Restore the chain** — add a fallback provider (DeepSeek → alternative) so retry-exhaustion and multi-hour outages fail over automatically. Requires a config write done by script, not `hermes config set`, because list-valued settings must be written as real YAML lists. Consistent with `2026-08-17-deepseek-client-data-approved`, which permits a fallback provider for reliability.
2. **Accept single-provider posture formally** — DeepSeek primary only; `wakeAgent` gate is the containment; outages surface as missed pulses. Would need a compensating control for the blind spot above (for example, a heartbeat that alerts when a pulse has not run for N hours).
3. **Defer again** — no change now. Weakest option available this time: the decision has already been deferred once, and the evidence base has not stood still.

## Questions for the 24 Sep review

1. Is a 21-day clean run, following 27 absorbed transients, enough to treat single-provider as settled?
2. Does the `wakeAgent` gate's silent-failure mode need a heartbeat alert regardless of which option is chosen?
3. If option 1: which provider, given DeepSeek is the approved carrier for client data?
4. Should the config's `'[]'` **string** form be corrected to a real empty list as a standalone hygiene fix, so a future fallback write is not silently ignored?

## Delta since this prep was written — verified 23 Sep 2026 22:00 ACST (review is tomorrow)

Re-verified the night before the review, so tomorrow's decision runs on current data rather than 17 Sep data.

| Check | 17 Sep | 23 Sep | Verdict |
|---|---|---|---|
| `fallback_providers` in config | `'[]'` (string) | **still `'[]'` (string)** — 30 days unchanged | No drift, no fix |
| `hermes fallback list` | — | **"No fallback providers configured."** | Single-provider posture intact |
| DeepSeek transients | 27 across 24–27 Aug, 21 clean days | **5 on 22 Sep** (4 × `APIConnectionError` on `deepseek-v4-flash`; 1 × `ReadError` `[Errno 32] Broken pipe` on `deepseek-v4-pro`), all absorbed at `attempt 1/3`, recovered | Retry still sufficient |
| Cron jobs non-ok | 1 of 42 | **3 of 42** — `LLM cost + balance check`, `Cron session prune`, `State DB snapshot backup` | **All three are `state.db` faults, none provider-related** |

**Clean-run count:** 18, 19, 20, 21, 23 Sep = **0** logged provider failures. The 22 Sep cluster is the only provider event in the 7 days, and it self-recovered inside the retry policy.

## The blind spot did not stay theoretical — it fired on 21 Sep

The prep's substantive argument was that the `wakeAgent` gate removes the alarm: an outage surfaces as *missed pulses*, quietly. That failure signature **materialised on 21 Sep — from a different root cause, with the same shape**:

- 15:36 ACST `quotes-pulse-phase2` read QPaint quote #43190 (msg `1a0c28fcd31b0811`), marked it **READ**, then died mid-extraction (`pandadoc-fresh-extract` short capture; `pandadoc-dom-pass` WebSocket timeout; session `end_reason None`).
- No record, no Telegram, no filing. Because the message was **no longer unread**, the `is:unread` wake gate correctly did **not** re-fire.
- The quote sat **read-but-unfiled for 3h36m**. Recovered 19:12 ACST by an `in:inbox` scan — not by any alert.

**Attribution (careful, per the prep's own standard):** the root cause was a crashed pulse run on a malformed `state.db`, **not** a DeepSeek outage. It is not provider evidence. What it *is* evidence of: **a stalled detection path produces no alarm**, on the exact control path that finds a QPaint customer reply. The 21 Sep window is the first measured instance, and it is the strongest available argument for the compensating heartbeat in verdict option 2 — regardless of which option is chosen.

The founder escalated `state.db` to customer-mail-control-path severity the same week (23 Sep). The 3h36m window is the primary evidence for that reclassification.

## Detection path — hardened since 17 Sep (relevant to option 2)

- **18 Sep:** `inbox-watchdog-gate.sh` hardened — Gmail returns per-minute quota exhaustion as HTTP 403 `rateLimitExceeded` (shared GCP project `206690510353`), which a single-shot curl turned into a **false "script failed" wake**. Now 4 attempts, linear backoff, fail-fast on 400/401/403, **fails OPEN**.
- **18 Sep (later):** the same gate's **token-refresh** step hardened — it runs *first*, before any API call, so query-layer retries never covered it. A broken pipe there would have blinded the primary customer-mail path behind a false "script failed".
- All three pulse gates (`inbox`, `quotes`, `james`) now retry and fail open. Verified against local stubs, not just by a green exit.

**Interpretation:** the gate layer is now robust against *transient* blindness. It has **no control against a stall that leaves the message read**, which is what 21 Sep produced. That is the specific gap option 2 must cover.

## What is still unchanged for tomorrow

- Verdict options 1 / 2 / 3 (Restore the chain / Accept formally with a heartbeat / Defer again) stand as written. Nothing in the 7 days makes deferral stronger — if anything, 21 Sep weakens it.
- The `'[]'` **string-vs-list** hygiene point stands and is worth doing regardless of the option chosen: a fallback write issued in the string form would be **silently ignored**.
- The 4 questions for the review stand unchanged.

## Evidence files

- `config.yaml` line 9 (`fallback_providers: '[]'`), `model:` block
- `cron/jobs.json` — 42 jobs, 1 non-ok
- `logs/gateway.log` — DeepSeek API-call failures 24–27 Aug, error-type census
- `logs/errors.log` — corroborating entries
- `knowledge/decisions/2026-08-24-provider-failover-deferred.md`
- `knowledge/decisions/2026-07-24-provider-failover-gemini-fallback.md`
- `knowledge/decisions/2026-08-17-deepseek-client-data-approved.md`
- `knowledge/decision-reviews/2026-08-24-provider-failover-review-prep.md` (the Aug review)
- `skills/devops/hermes-gateway-ops/SKILL.md` — 29 Aug outage attribution
- `logs/quotes-pulse.log` — 21 Sep 19:12 ACST entry: the 3h36m read-but-unfiled incident (#43190)
- `learnings/2026-09-23-state-db-customer-mail-control-path.md` — escalation of the `state.db` fault to customer-mail-path severity
- `learnings/LEARNINGS.md` 2026-09-18 — pulse-gate hardening entries (three gates retry + fail open)

## Day-before refresh

The "Delta since this prep was written" section above was added by the Prepare-Ahead Clock on
23 Sep 2026 22:00 ACST — **the night before the review** — re-verifying every load-bearing number
against live config, logs and cron state rather than re-serving the 17 Sep figures.

---

*Prepared by the Prepare-Ahead Clock, 17 Sep 2026 22:00 ACST, 7 days before the review date. No contact made, no config changed, no CRM records changed.*
