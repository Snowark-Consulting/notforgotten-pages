# James Inbox — Read-Only, On-Demand Only

> ⚠️ **GUARDRAIL: CRITICAL.** This integration is wired as read-only, on-demand only. Never access without explicit instruction from Josh or James.

## Access

| Field | Value |
|-------|-------|
| Account | `james@notforgotten.com.au` |
| Token | `/root/.hermes/james_token.json` |
| Scope | `https://www.googleapis.com/auth/gmail.readonly` ONLY |
| Auth method | Dedicated OAuth (James Hardstaff's Google account) |
| Can send? | **NO** — `gmail.send` scope not granted |
| Can modify? | **NO** — `gmail.modify` scope not granted |

## Guardrails (NEVER BREAK)

1. **On-demand only.** Never pull from this inbox in any automated cron, pulse, or background job. No exceptions.
2. **Explicit instruction required.** Only access when Josh Hancock or James Hardstaff directly say "check James's inbox" or equivalent.
3. **No auto-summarising.** Do not volunteer information from James's inbox unless asked.
4. **No forwarding, no replying, no marking read.** Token is read-only by scope, but do not attempt even read-only operations without permission.
5. **Token isolation.** This token lives at `james_token.json` — completely separate from the Atlas inbox token at `google_token.json`. Never mix them.

## Usage (when authorised)

```python
import json, urllib.request

with open("/root/.hermes/james_token.json") as f:
    token = json.load(f)["token"]

req = urllib.request.Request(
    "https://gmail.googleapis.com/gmail/v1/users/me/profile",
    headers={"Authorization": f"Bearer {token}"}
)
```

Or use the Atlas inbox check script with the token path override.

## Why read-only?

Josh Hancock (2026-06-24): "Can it be read only?" — yes, and it is. The OAuth flow requested only `gmail.readonly`. James cannot accidentally have mail sent or modified through this token.

## Cron / automation exclusion

The inbox pulse cron (`inbox-watchdog-phase1` + `inbox-pulse-phase2`) monitors `atlas@notforgotten.com.au` only via `google_token.json`. James's token is NOT referenced in any cron, script, or skill that auto-executes. If a future cron is proposed that touches this token, it requires explicit approval from Josh or James.
