# Learnings Catalogue

> Loaded at every session boot. Before touching any external API, tool, or domain, scan this table. If the domain you're about to touch has a card, load it first. One domain = one card. Cards load on-demand, not at boot.
>
> **EDITING THIS TABLE:** never fuzzy-patch multi-line blocks here — full-row replacement only, or rewrite the whole file. This table has been corrupted **17 times** by partial edits (dropped rows, `||` prefix damage, mid-table splits). Repair history: 2026-09-06, 2026-09-07 (consolidation), 2026-09-09 (consolidation), 2026-09-10 (consolidation), 2026-09-11 (consolidation), 2026-09-12 (consolidation), 2026-09-13 (consolidation), 2026-09-14 (consolidation), 2026-09-15 (consolidation), 2026-09-16 (consolidation), 2026-09-17 (consolidation), 2026-09-18 (consolidation), 2026-09-20 (consolidation), 2026-09-21 (consolidation), 2026-09-22 (consolidation), 2026-09-23 (consolidation). **ATTRIBUTION (corrected twice — read this carefully):** the recurring damage is a write landing at **~21:33–21:38 UTC = ~07:03–07:08 ACST the following morning**, roughly 10 hours AFTER the 21:20 ACST guard and the 21:30 ACST backup — never inside their window. Two earlier diagnoses were wrong and are recorded so they are not repeated: (a) "the guard ran before the write", which was a UTC-mtime-read-as-Adelaide-local error, and (b) the 2026-09-14 claim that the window contains **no** tool calls. It does: 13 calls from `quotes-pulse-phase2` quote intake were logged there on 2026-09-14 21:37 UTC. **None of them read or write anything under `learnings/`**, and the corruption's rewritten prose appears nowhere in `state.db` — so the writer is still outside the logged agent loop. **The 10th event (2026-09-15) signature:** warning block deleted, `gmail-auth` + `qpaint` rows dropped, 7 rows `||`-prefixed (`hermes`, `shell`, `cloudflare`, `compliance`, `agent-architecture`, `browser`, `canonical-send`), the `campaign` cell rewritten as a markdown link, a mid-table blank line inserted, and in `cards/agent-architecture.md` + `cards/shell.md` three `## TRAP` headings lost and two traps silently reworded. **The 11th event (2026-09-16) signature — the first provably tool-call-free window:** the write landed **21:36:00.51–21:36:01.92 UTC = 07:06:00–07:06:02 ACST**, exactly two files 1.4 s apart (`catalogue.md`, `cards/agent-architecture.md`); `logs/tool-calls.jsonl` held **ZERO** entries across 21:30–21:50 UTC while demonstrably live (its tail is that same night's consolidation reading the damage); the only jobs producing output in the window were three `no_agent` script crons — Client OS rebuild 21:30:59, QPaint Completed-Label Reverse Control 21:31:29, Dashboard hourly refresh 21:31:54 — all clean and silent, none touching `learnings/`; a whole-disk grep for the corruption's distinctive strings returned only readers (`state.db` read-backs, prior investigation probes), never a writer's payload; and the damaged card **renamed** the out-of-band heading (restore from HEAD, never rename by hand). **The 12th event (2026-09-17) signature — identical to the 11th:** the write landed **21:37:04.48–21:37:05.83 UTC = 07:07:04–07:07:05 ACST**, the same two files 1.4 s apart, roughly 10 h after the 21:20 guard and 21:30 backup both ran clean; the guard reported **31 findings** on the damaged tree (vs 30 on the 11th). Repair = `git restore` both files from HEAD plus a **duplicate check before re-applying anything** — the writer's only genuinely-new block (a "customer reply already completed = LOST / third-party dependency = Engaged + deferred" trap) was already canonical in `skills/email/email-inbox-pulse/references/qpaint-customer-reply-processing.md`, so it was deliberately NOT re-added to the card. **The mechanical guard `scripts/bin/learnings-integrity-guard.py` detects this class automatically — run it as the LAST step of every consolidation** (its 21:20 cron cannot see work written after it). Because the damage lands ~07:03 ACST, no same-evening reschedule can catch it — rec `consolidation-learnings-guard-0715` proposes a ~07:15 ACST guard run. **The 13th event (2026-09-18) signature:** three files this time, not two - `catalogue.md` (2026-09-17 21:32:46 UTC) and `cards/hermes.md` (21:32:47, 1.1 s later) in the usual burst, plus `cards/gmail.md` at 21:56:23 UTC (~07:26 ACST). Catalogue damage identical to prior events: warning block deleted, `gmail-auth` + `qpaint` rows dropped, 7 rows `||`-prefixed (`hermes`, `shell`, `cloudflare`, `compliance`, `agent-architecture`, `browser`, `canonical-send`), `campaign` cell rewritten as a markdown link, mid-table blank line inserted. `cards/hermes.md` lost 2 `## TRAP` headings (`v0.20.4 session turn lease`, `Gateway lifecycle guard false-flags python3 -c`) plus the `UPDATE 2026-08-18` note; `cards/gmail.md` lost the `Customer-quoted date` trap outright and had the 403-quota trap condensed. Guard on the damaged tree = **38 findings** (vs 31 on the 12th). Repair = `git restore` all three from HEAD, then re-apply only the genuinely-new blocks after a duplicate check: the OAuth-refresh single-shot trap (gmail) and the `write_file` append-only-log trap (hermes) - both were new to the CARD layer, unlike the 12th event where the only new block was already canonical in a skill reference. The writer's reworded copies of existing traps were NOT re-applied. **The 14th event (2026-09-20) signature — the widest and most destructive yet:** **four files in a 5.7 s burst** — `catalogue.md` (2026-09-19 21:38:22 UTC = 07:08:22 ACST), `cards/gmail.md` (21:38:24), `cards/agent-architecture.md` (21:38:26), `cards/qpaint.md` (21:38:28). HEAD had been committed clean at **07:00:50 ACST, 8 minutes before the write**, so the damage landed after BOTH the 06:50 guard and the 07:00 backup and was found by the 07:30 weekly lint — the first control to see it. Fourteen traps were deleted across three cards; **`cards/qpaint.md`, a live paying client's operating rules, went from 6 traps to 1** (all six HEAD traps lost, replaced by a single status-blind trap). `cards/gmail.md` lost 5 traps plus a `**Symptom:**` update note — learning written one and two days earlier. Catalogue damage followed the familiar pattern: warning block deleted, `gmail-auth` row dropped, 8 rows `||`-prefixed, mid-table blank line, `campaign` cell turned into a markdown link. Guard on the damaged tree = **44 findings** (31 → 38 → 44 across the last three events). Repair = `git restore` the four files from HEAD, then extract → duplicate-check → re-apply: the status-blind trap (qpaint) and the negative-grep trap (agent-architecture) were new to the CARD layer and WERE re-applied; the gmail rewrites and the "customer reply already completed = LOST / third-party dependency = Engaged + deferred" block were already canonical elsewhere and were NOT. **The 15th event (2026-09-21) signature - five files, the widest file-count yet:** `cards/python.md` (2026-09-20 21:52:04.1 UTC = 07:22:04 ACST), `cards/gmail.md` (21:52:06.4, -2.3 s), `cards/agent-architecture.md` (21:52:09.0), `cards/shell.md` (21:52:11.4), `catalogue.md` (21:52:13.4) - five files in a 9.3 s burst, ~10 h after the 21:20 guard and the 21:30 backup, found by that night's consolidation. Damage followed the familiar pattern (warning block deleted, `gmail-auth` row dropped, 9 rows `||`-prefixed, mid-table blank line, `campaign` cell as a markdown link) plus `cards/gmail.md` lost 5 traps and a `**Symptom:**` update note, `cards/python.md` lost the `/tmp` stdlib-shadow trap, `cards/shell.md` lost the glob-delete trap, and `cards/agent-architecture.md` lost the customer-deferral and negative-grep trap headings. Guard on the damaged tree = **41 findings** (44 -> 41: more files, fewer findings - the finding count is not a severity proxy). Repair = `git restore` all five from HEAD, then extract -> duplicate-check -> re-apply: the email-signature trap (gmail) and the multi-week-mean trap (agent-architecture) were attested by `LEARNINGS.md` and were re-applied; a macOS `arm64e.x1` linker trap in the writer's `cards/shell.md` had **no attestation anywhere on disk** and was NOT re-applied; the condensed rewrites of existing traps were NOT re-applied. **The detection gap is now the dominant cost** — the damage is ~24 h old before any control sees it, and the guard cron itself exits 0 while reporting corruption. Rec `consolidation-learnings-guard-0715` (a ~07:15 ACST detect-and-restore run) remains the countermeasure and is still unbuilt. **The 16th event (2026-09-22) signature — the record file count, and the first fabricated-infrastructure block:** SIX cards plus the catalogue — `cards/cron.md` (2026-09-21 21:42:32 UTC = 07:12:32 ACST), `cards/gmail.md` (21:42:35), `cards/hermes.md` (21:42:38), `cards/qpaint.md` (21:42:39), `cards/shell.md` (21:42:41), `cards/unclassified.md` (21:42:43, a NEW card created by the writer), `catalogue.md` (21:44:25) — an 11 s burst then the catalogue 1 m 42 s later, with `LEARNINGS.md` touched at 22:05:41 UTC (a legitimate pure addition, verified). Guard on the damaged tree = **50 findings** (30 -> 31 -> 38 -> 44 -> 41 -> 50). Damage: warning block deleted, `gmail-auth` row dropped, `qpaint` row rewritten, `hermes` row `|||`-prefixed, 7 more rows `||`-prefixed, `campaign` cell as a markdown link, mid-table blank line; **`cards/qpaint.md`, the live paying client's card, went 7 traps -> 2**; `cards/gmail.md` lost 5 traps + a `**Symptom:**` note; `cards/hermes.md` lost 2 traps + the `UPDATE 2026-08-18` note; `cards/shell.md` lost the glob-delete trap. **New element — the writer now fabricates infrastructure:** its `cards/cron.md` block asserts a `scripts/cleanup-cron-output.sh`, a `no_agent` cron `c3cb0224f2ce` ("Cron output retention", daily 22:45) and an update to "consolidation-sop.md step 0.6" — **none of the three exist**. Repair = `git restore` all six from HEAD + delete the invented `unclassified.md`, then extract -> **ATTEST** -> duplicate-check -> re-apply: only **2 of 9** writer blocks passed attestation (Pulse crash after mark-read -> gmail; QPaint tick-box OPTION -> qpaint), both attested by `LEARNINGS.md` (lines 301 / 308) and by `state.db` rows predating 21:42:32 UTC. The other 7 had **zero pre-write evidence on disk** and were NOT re-applied; the cron-output-retention block was additionally false. Verified by heading LIST diff (additions only) and a guard re-run = silent PASS. **The 17th event (2026-09-23) signature — the clean-HEAD baseline was destroyed:** three files in a **4-second burst** — `catalogue.md` (2026-09-22 21:36:36 UTC = 07:06:36 ACST), `cards/hermes.md` (21:36:38), `cards/email.md` (21:36:40, a NEW card created by the writer). Guard on the damaged tree = **21 findings** (50 -> 21; the count tracks how much HEAD-based diff still works, not severity). Damage: warning block deleted, `gmail-auth` row dropped, 9 rows `||`-prefixed, `campaign` cell as a markdown link, mid-table blank line; `cards/hermes.md` lost the `UPDATE 2026-08-18` note plus 2 traps (`v0.20.4 session turn lease`, `Gateway lifecycle guard false-flags python3 -c`) and the 13th-event fix-list items, and gained 5 blocks of which only 1 was attested. **The new binding problem: `git restore` from HEAD no longer works.** The vault backup was re-homed to `Hardstaff08/notforgotten-vault` on 2026-09-23 08:50 ACST as a fresh single-commit snapshot, and an hourly job had already `git add -A`-ed the 07:06 corruption into the old history, so the damage was committed. **Repair source is now a pre-write commit recovered from the old pack** (`git cat-file --batch-all-objects` -> filter unreachable commits -> pick the newest with a structurally clean catalogue); grep for the `EDITING THIS TABLE` marker, not the commit date. `nightly-backup.sh`'s pre-commit gate does NOT protect against this because the hourly commit is a different job. Rec `consolidation-learnings-clean-baseline-lost`.

| Domain | Trigger keywords | Card |
|--------|-----------------|------|
| gmail | `gog gmail`, `newer_than`, `batchModify`, `mark.read`, `threadId`, `Gmail API` | `cards/gmail.md` |
| gmail-auth | `gmail_auth.py`, `get_access_token`, `OAuth refresh`, `access token`, `401`, `google_token.json` | `cards/gmail-auth.md` |
| email | `MIMEApplication`, `MIMEImage`, `Content-Type`, `attachment`, `email send` | `cards/email.md` |
| hermes | `shell hook`, `matcher`, `post_tool_call`, `pre_tool_call`, `pre_llm_call`, `allowed_users`, `allowlisted`, `state.db`, `system_prompt`, `skill-injection`, `trigram`, `date hook`, `date integrity`, `inject-date`, `autorestart`, `supervisord`, `supervisorctl`, `write_file`, `append-only log` | `cards/hermes.md` |
| shell | `zsh`, `for p in`, `\| tee`, `tmux pipe`, `nvm`, `pyenv`, `echo $`, `declare -A`, `terminal`, `background` | `cards/shell.md` |
| cron | `cronjob`, `schedule`, `launchd`, `LaunchAgent`, `crontab`, `keychain`, `wakeAgent`, `wakeagent`, `script gate`, `token burn` | `cards/cron.md` |
| telegram | `allowed_chats`, `allowed_users`, `TELEGRAM`, `@mention`, `/start`, `bot` | `cards/telegram.md` |
| orgo | `orgo.ai`, `bash API`, `504`, `gateway`, `UUID`, `computer` | `cards/orgo.md` |
| supabase | `supabase`, `anon key`, `service_role`, `client_responses`, `RLS` | `cards/supabase.md` |
| git | `git push`, `git clone`, `PAT`, `case.sensitive`, `render`, `template` | `cards/git.md` |
| models | `gemini`, `model`, `deprecated`, `discontinued`, `404`, `provider` | `cards/models.md` |
| campaign | `cold-email`, `campaign`, `calibration`, `prospect overlap`, `ownership check`, `pre-flight` | `cards/campaign.md` |
| cloudflare | `cfat_`, `account-owned`, `Cloudflare`, `api.cloudflare`, `tokens/verify` | `cards/cloudflare.md` |
| compliance | `ABN`, `GST`, `TFN`, `ATO`, `trust`, `Australian Business Register`, `ABR` | `cards/compliance.md` |
| python | `datetime.now`, `ZoneInfo`, `timezone`, `Adelaide time`, `UTC server`, `bare datetime` | `cards/python.md` |
| agent-architecture | `dual-path`, `skill discovery`, `pre-flight`, `orphaned skill`, `boot context`, `Registry`, `SOPs`, `certify`, `verify`, `certification gate`, `touchpoint`, `review point`, `auto-contact`, `CRM stage`, `customer evidence`, `commercial truth`, `materiality`, `dream`, `consolidation`, `classifier`, `false positive`, `MONITOR`, `courtesy reply`, `negative grep`, `rule does not exist` | `cards/agent-architecture.md` |
| asana | `asana`, `html_notes`, `POST /tasks`, `PUT /tasks`, `html_lacks_body`, `body tag` | `cards/asana.md` |
| airtable | `airtable`, `Airtable`, `record`, `duplicate`, `pagination`, `maxRecords`, `pageSize` | `cards/airtable.md` |
| browser | `Accessibility.getFullAXTree`, `CDP`, `AX tree`, `React SPA`, `contenteditable`, `DOM invisible`, `browser_console tab`, `Page.navigate`, `chrome.newtab` | `cards/browser.md` |
| canonical-send | `canonical-send`, `find_approval`, `Gate 1`, `FOUNDER_APPROVED`, `hash mismatch`, `canonical.email.send` | `cards/canonical-send.md` |
| microsoft-graph | `Microsoft Graph`, `$filter`, `isRead`, `ODATA`, `Graph API`, `msgraph` | `cards/microsoft-graph.md` |
| qpaint | `QPaint`, `1300 QPAINT`, `white-label`, `Dee`, `Leslie`, `Tas Moulis`, `pipeline truth`, `recovery ops`, `Email 3`, `status-blind`, `ack` | `cards/qpaint.md` |

## How to use

1. **Agent discovers a fix** → appends to own `learnings/LEARNINGS.md` (existing process).
2. **Nightly consolidation** → notes new learning in `notes/<date>.md` with `**New learning:** <title>`.
3. **Winston's morning harvest** → polls all agents' daily notes, extracts new learnings, formalizes into a card update, pushes to deploy kit, and redistributes to all agents.
4. **Every agent** → loads this catalogue at boot. Before touching a domain, checks the trigger column. If keyword matches, loads the card.

## Card format

Each card in `cards/<domain>.md`:
```markdown
# <domain> — Known Traps

## TRAP — short title
**When:** what action triggers this
**Symptom:** what breaks
**Fix:** exact working approach
```
